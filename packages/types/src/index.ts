/**
 * @boltbet/types
 *
 * Shared TypeScript types consumed by both the API and the Web app.
 * Only stable, agreed contracts live here — no internal implementation details.
 *
 * Add types here when a shape needs to be used in more than one package
 * (e.g. an API response DTO that the frontend also needs to deserialise).
 */

// Re-export shared enums as they stabilise
// export * from './betting';
// export * from './wallet';
// export * from './identity';
