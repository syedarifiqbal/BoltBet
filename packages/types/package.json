{
  "name": "@boltbet/types",
  "version": "0.0.1",
  "description": "Shared TypeScript types for BoltBet — API contracts, enums, payloads",
  "private": true,
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc --noEmit",
    "lint": "echo 'no linting configured for types package yet'"
  },
  "devDependencies": {
    "typescript": "^5.7.3"
  }
}
